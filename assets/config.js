// Supabase configuration.
// Leave these empty for LOCAL mode: submissions are saved to a downloadable
// JSON file and logged to the console instead of being sent anywhere.
// For deployment, fill in your project URL and anon (public) key.
window.SSM_CONFIG = {
  SUPABASE_URL: "",      // e.g. "https://xxxx.supabase.co"
  SUPABASE_ANON_KEY: "", // the public anon key (safe to expose)
};
